import {AppActionTypes} from "../store";

// Action creators type

// All action types
export type CardsActionTypes = any // fix any

// State type
export type CardsStateType = typeof initialState

// State
const initialState = {

}

// Reducer
export const cardsReducer = (state = initialState, action: AppActionTypes): CardsStateType => {
  switch (null) {

    default:
      return state
  }
}

// Action creators

// Thunk creators